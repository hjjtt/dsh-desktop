import { clientBundle } from '../tsdown.client.ts'

/** Browser half only: the sidebar file-tree surface. */
export default clientBundle('@deepseek-ai/dsh-client-ui-file-tree', ['lib/types/index.js', 'lib/types/invariant.js'])
